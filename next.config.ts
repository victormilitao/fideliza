import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // TailwindCSS v4 works natively with Next.js
  // Turbopack is enabled by default in Next.js dev
}

export default nextConfig
